Program Pzim ;

 var
     i : Integer;
     e : Integer;
     nome : String;
 Begin
 
    for i := 1 to 10 do
    begin
        writeln('RL System ', i);
    end;
    
    for e := 1 to 20 do
    begin
        readln(nome);
    end;
  
  // i : Indice
  // Estrutura de Repetição | Loop
 End.
